# 1. Virtual Environments (สำคัญมาก เพื่อไม่ให้ไฟล์ Library หนักๆ หลุดขึ้นไป)
fraud_final/
venv/
env/

# 2. Python Cache
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
# 3. Data Files (ป้องกันไฟล์ข้อมูลขนาดใหญ่)
data/*.zip
data/*.csv
data/*.gz
artifacts/

# 4. Model & Parameters (ถ้าไฟล์ใหญ่เกินไป หรือต้องการเก็บไว้แค่ในเครื่อง)
# models/*.joblib
# parameters/*.joblib

# 5. IDE & OS Files
.vscode/
.idea/
.DS_Store
Thumbs.db

# 6. Environment Variables (ป้องกันข้อมูลความลับ)
.env